======================================================================
VALID PARSER TEST SUMMARY: 9/12 tests passed
======================================================================

FAILURES: These files should parse successfully but failed

[FAILED] any_expr_valid.platter
----------------------------------------------------------------------
Code:
    1 | start() {
    2 |     j = (x + 12.32 > 32.2);
    3 | }

Error Output:
Syntax Error: Unexpected '>' at line 2, col 20. Expected '+', '-', '*', '/', ')'.
======================================================================

[FAILED] arith_log_rel_valid.platter
----------------------------------------------------------------------
Code:
    1 | start() {
    2 |     flag of b = not a and not b;
    3 |     flag of b = not (up or down);
    4 |     flag of b = up or not not down;
    5 |     flag of b = not a or not b and c or not x ;
    6 |     flag of b = a or b ;
    7 |     flag of b = not y and not z ;  
    8 |     flag of b = upArray[0] or upArray[0] ;
    9 |     
   10 |     flag of b = a == 3; 
   11 | 	flag of b = "Hello" == "Hello";
   12 | 	flag of b = (x <= y) == (x == z);
   13 | 	flag of b = tosip(p) <= r;
   14 | 	flag of b = (a>b)==(b<p);
   15 | 	flag of b = flagArray[0] != flagArray[1];
   16 | 
   17 | 
   18 | 	bill(tochars(x + c * y));
   19 | 	bill(tochars((x - y) % num   ));
   20 | 	bill(tochars(x / num + y  ));
   21 | 	bill(tochars((c - num) * (x - y)   ));
   22 | 	bill(tochars(sip2 / sip1 + sip3   ));
   23 | 	bill(tochars((sip3 - sip2) * sip1 ));
   24 | 	bill(tochars((x % c) + (y % num) ));
   25 | 	bill(tochars(numArray[0] + numArray[1] ));
   26 |     a = numArray[0] + sipArray[0];
   27 |     
   28 | }

Error Output:
Syntax Error: Unexpected '==' at line 12, col 23. Expected 'and', 'or', ',', ';'.
======================================================================

[FAILED] complex_charexpr_assignop_valid.platter
----------------------------------------------------------------------
Code:
    1 | start() {
    2 |     flag of a = (3 + 5) * 2 > 10 and up;
    3 | 	flag of b = ((up) or (up or down)) == up;
    4 | 	flag of c = not (4 % 2 == 1);
    5 | 	piece of d = x + pieceArray[0] * pieceArray[z];
    6 |     flag of e = ((20 / 5) + 3) * 2 <= 14 or (18 - 10) % 4 == 1;
    7 | 	flag of f = (50 / (5 * 2)) + (12 % 5) < 10 or (6 * 3) - 4 == 14;
    8 | 	flag of g = not (x >= y) and not (y >= x);
    9 | 	flag of h = (double(3) + 5) * 2 >= 10 * 2 + 2;
   10 | 	flag of i = ((x + y) < z) and q == 4;
   11 | 	flag of j = (x > y) == (x < z) and up or not down and (5+ 2) >= 7 or not down;
   12 |     
   13 |     chars of k = tochars(age) + " years old";
   14 | 	chars of l = greet + " " + "world";
   15 | 	chars of m = "Identifiers are used" + "to name user-defined ingredients, " + "recipes, arrays, spices, and tables.";
   16 | 	chars of n = "hi" + name;
   17 | 	chars of o = charArray[0] + charArray[1];
   18 |     
   19 |     seven = one+12;
   20 | 	three += one;
   21 | 	four -= six + tosip(three);
   22 | 	six *= 7.7;
   23 | 	one /= 51;
   24 | 	one %= three;
   25 | 	pieceArray[3] += 6/3*2;
   26 | 
   27 | 
   28 | 
   29 | 
   30 |  
   31 | }

Error Output:
Syntax Error: Unexpected 'or' at line 3, col 20. Expected ')'.
======================================================================

