# Square root of input number (using sqrt() built in recipe)

start(){
        sip of n;
    bill("Enter a number: ");
    n = tosip(take());
           bill("Square root of "+tochars(n)+" is "+ tochars(cut(sqrt(n),1.0)));
}
--end
1225
--end
Enter a number: 1225
Square root of 1225.0 is 35