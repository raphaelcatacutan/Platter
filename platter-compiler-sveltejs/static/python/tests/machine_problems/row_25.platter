prepare piece of reverseDigit(piece of num){
	piece of r = 0;
    repeat(num != 0){
    	piece of rem = num % 10;
        r = r * 10 + rem;
        num = num / 10;
    }
    serve r;
}

start(){
	bill(tochars(reverseDigit(topiece(take()))));
}
--end

--end
