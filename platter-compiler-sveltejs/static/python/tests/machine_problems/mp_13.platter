# Sum of the cube of the numbers from 1 to N

start(){
        piece of i,n,sum;
    bill("Enter a number: ");
    n = topiece(take());
    pass(i = 1; i+=1; i<=n){
            sum += i*i*i;
    }
    bill("The sum is "+tochars(sum));
}
--end
10
--end
Enter a number: 10
The sum is 3025