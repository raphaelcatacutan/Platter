error on the compute() recipe call (this is a unique shadowing scenario in platter, platter allows the use of an identifier that is used by a recipe as long as they are not used in the same scope, this particular program has both set, only 1 could exist, per scope, and additionally, compute ingredient is not even declared so it is probably currently reading the global compute id which is a recipe and not an ingredient which is wrong)