start(){
        piece of x = 18;
        flag of hasID = up;
    check(x >= 18){
            check(hasID){
                bill("okay");
        }
    instead{
                bill ("noo");
    }
    }
}